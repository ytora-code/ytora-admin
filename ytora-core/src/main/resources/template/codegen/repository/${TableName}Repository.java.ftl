package ${path}.${module}.repository;

import org.ytor.common.util.Colls;
import org.ytor.common.util.Strs;
import org.ytor.core.sqlflow.BaseRepository;
import org.ytor.core.sqlflow.SQLHelper;
import org.ytor.core.sqlflow.builder.select.WhereStage;
import ${path}.${module}.model.entity.${TableName};

/**
 * created by ${currentUser} on ${.now}
 * <br/>
 * ${TableComment}表 Repository 类
 * <br/>
 */
public abstract class ${TableName}Repository extends BaseRepository<${TableName}> {

}